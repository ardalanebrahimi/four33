name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Create environment files
        run: |
          cat > src/environments/environment.ts << 'EOF'
          export const environment = {
            production: false,
            apiUrl: 'https://four33-api-dmgddtanecdzbnh8.westeurope-01.azurewebsites.net/api',
            googleClientId: '693414286728-sd23gnij8c78033bmi10atoig3nj5007.apps.googleusercontent.com',
          };
          EOF
          cat > src/environments/environment.prod.ts << 'EOF'
          export const environment = {
            production: true,
            apiUrl: 'https://four33-api-dmgddtanecdzbnh8.westeurope-01.azurewebsites.net/api',
            googleClientId: '693414286728-sd23gnij8c78033bmi10atoig3nj5007.apps.googleusercontent.com',
          };
          EOF

      - name: Build
        run: npm run build -- --configuration production --base-href /

      - name: Add 404.html for SPA routing
        run: cp dist/four33/browser/index.html dist/four33/browser/404.html

      - name: Add CNAME for custom domain
        run: echo 'four33.app' > dist/four33/browser/CNAME

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'dist/four33/browser'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
